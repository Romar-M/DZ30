[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lms_project"
version = "0.1.0"
dependencies = [
    "django>=5.0",
    "djangorestframework>=3.15",
    "Pillow>=10.0",
]